"use client";
import type { Post } from "@/types";

interface Props {
  initialPosts: Post[];
}

// scroll on demand component
export default function PostsPresentor({ initialPosts }: Props) {
  throw new Error("Not yet implemented.");
}
